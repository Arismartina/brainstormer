# .gitpod.Dockerfile for Brainstormer project
FROM gitpod/workspace-full-vnc:latest # Or a similar suitable base image, e.g., gitpod/workspace-full

# You can add global packages here if needed, but we'll use init tasks for project-specific ones
# USER gitpod
# RUN npm install -g some-global-package
